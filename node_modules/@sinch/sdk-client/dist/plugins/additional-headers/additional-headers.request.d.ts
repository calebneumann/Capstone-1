import { PluginRunner } from '../core';
import { RequestOptions, RequestPlugin } from '../core/request-plugin';
export interface AdditionalHeaders {
    headers: Promise<{
        [key: string]: string;
    }>;
}
/**
 * Builds a Promise containing the new header in the form of an object {key : value}
 * @param {string} key - the header key
 * @param {string} value - the header value
 */
export declare const buildHeader: (key: string, value: string) => Promise<{
    [key: string]: string;
}>;
export declare class AdditionalHeadersRequest implements RequestPlugin {
    private readonly additionalHeaders;
    constructor(additionalHeaders: AdditionalHeaders);
    getName(): string;
    load(): PluginRunner<RequestOptions, RequestOptions>;
}
